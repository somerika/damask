@AGENTS.md

# Formatting

After every change to source files, run Biome to format:

```
npm run format
```

This formats all JS/TS/TSX files with 2-space indent, single quotes, and no unnecessary semicolons.
